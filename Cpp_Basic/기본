#include <iostream> 
/*
전처리기.
'#'이 줄 맨 앞에 붙은 명령어를 총칭.
컴파일을 시작할 때 가장 먼저 실행. (컴파일 우선순위가 높음)
프로그램 기능 구현에 직접적인 도움을 주지는 않지만, 기능을 구현하는 것을 돕는 역할 (실제 코드들이 기능을 구현하기 전에 먼저 나서는 서포팅 코드)

#include
헤더 파일에 있는 내용들을 해당 코드에 포함 시킴.
#include <> : 표준 헤더(컴파일러에 설정되어있는 특정 디렉토리) 검색
#include "" : 표준 헤더 이외의 경로 검색. 사용자 정의 헤더

#define
특정 문자열을 특정 값으로 정의.
#define A B : A를 B로 치환
#define A : A를 정의(값 X)
#define SQUARE(x) ((x)*(x)) : 함수 매크로 정의 (해당 매크로 수식 사용 시 연산 순서가 바뀔 수 있기 때문에 괄호로 잘 묶어서 사용)

#undef A : A 정의 해제

#ifdef. #else. #endif
if~else 문 처럼 사용

#ifndef : #ifdef의 반대. 정의되지 않았는지 여부 확인

#pragma once : 해당 소스를 단 한 번만 컴파일
*/

int main(int argc, char *argv[])
/*
int argc : main 함수에 전달되는 데이터의 개수
char *argv[] : main 함수에 전달되는 실제 데이터. char형 포인터 배열로 구성. 첫 번째 문자열은 프로그램의 실행 경로.
*/
{
  
  return 0; // main 함수의 자료형이 int이기 때문에 반환값은 정수형이어야 함. void main()형태였다면 반환값이 없음.
}
